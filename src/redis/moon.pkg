import {
  "moonbitlang/async",
  "moonbitlang/async/socket",
  "moonbitlang/async/aqueue",
  "moonbitlang/core/string",
}

options(
  "proof-enabled": true,
  targets: {
    "client.mbt": [ "native" ],
    "client_test.mbt": [ "native" ],
    "pool.mbt": [ "native" ],
  },
)
